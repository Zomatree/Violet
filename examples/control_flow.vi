import { print } from std;

fun test(i: Integer, j: Integer): Boolean {
	return i <= j;
}

fun main() {
	let x = "if?";
	print(x);
	if (test(1, 0)) {
		let y = "IF!";
		print(y);
	}
	elseif (test(1, 1)) {
		let y = "ELSEIF!";
		print(y);
	}
	elseif (test(1, 2)) {
		let y = "ELSEIF2!";
		print(y);
	}
	else {
		let y = "ELSE!";
		print(y);
	}
}
